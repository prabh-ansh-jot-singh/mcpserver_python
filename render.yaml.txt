services:
  - type: web
    name: mcp-server
    runtime: python
    pythonVersion: "3.9.13"
    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: gunicorn --worker-tmp-dir /dev/shm --workers 2 --threads 4 --timeout 60 app:app
    envVars:
      - key: GCP_CREDENTIALS
        value: your_service_account_json_here
    healthCheckPath: /health
    autoDeploy: true
    plan: free
